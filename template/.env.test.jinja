DEBUG=false
SECRET_KEY="insecure-test-secret-key"
ALLOWED_HOSTS=localhost,127.0.0.1,::1
# CSRF_TRUSTED_ORIGINS entries must include both scheme and hostname
CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1
{%if is_github_project %}
# TODO: if updating the password below, update it in the GitHub action specs as well
{%endif%}
DATABASE_URL="postgres://test_{{project_name}}:password@localhost:5432/test_{{project_name}}"
