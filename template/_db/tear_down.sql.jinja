/**
 Usage:
 ```
 psql \
   -U $(whoami) \
   -d postgres \
   -f _db/tear_down.sql
 ```
**/

SELECT pg_terminate_backend(pid)
FROM pg_stat_activity
WHERE datname = '{{project_name}}'
  AND pid <> pg_backend_pid();

DROP DATABASE IF EXISTS {{project_name}};

DROP USER IF EXISTS {{project_name}};

SELECT pg_terminate_backend(pid)
FROM pg_stat_activity
WHERE datname = 'test_{{project_name}}'
  AND pid <> pg_backend_pid();

DROP DATABASE IF EXISTS test_{{project_name}};

DROP USER IF EXISTS test_{{project_name}};
